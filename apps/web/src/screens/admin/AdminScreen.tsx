import { ScreenStub } from '@/components/ScreenStub';

// Worker (admin panel): KPIs (Recharts), tablas (@tanstack/react-table), verificar guías, reportes.
export function AdminScreen() {
  return <ScreenStub unit="Admin" title="Panel de administración" />;
}
