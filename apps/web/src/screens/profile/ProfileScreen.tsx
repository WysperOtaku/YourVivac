import { ScreenStub } from '@/components/ScreenStub';

// Worker (perfil+ajustes): perfil user/guide (badge terra), contadores, logros.
export function ProfileScreen() {
  return <ScreenStub unit="Perfil" title="Tu perfil" />;
}
