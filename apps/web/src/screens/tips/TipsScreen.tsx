import { ScreenStub } from '@/components/ScreenStub';

// Worker (consejos+publicar): feed de tips + modal editor Markdown.
export function TipsScreen() {
  return <ScreenStub unit="Consejos" title="Consejos de montaña" />;
}
