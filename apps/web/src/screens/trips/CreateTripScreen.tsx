import { ScreenStub } from '@/components/ScreenStub';

// Worker (crear+detalle salida): formulario rhf+Zod + invitar.
export function CreateTripScreen() {
  return <ScreenStub unit="Crear salida" title="Nueva salida" />;
}
