import { ScreenStub } from '@/components/ScreenStub';

// Worker (home): feed de "tu cordada", carrusel de salidas, ranking.
export function HomeScreen() {
  return <ScreenStub unit="Inicio" title="Tu cordada" />;
}
