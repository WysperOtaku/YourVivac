import { ScreenStub } from '@/components/ScreenStub';

// Worker (equipo+productos): biblioteca de listas, filas con etiqueta de tienda, buscador.
export function GearScreen() {
  return <ScreenStub unit="Mi equipo" title="Biblioteca de listas" />;
}
