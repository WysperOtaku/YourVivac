import { View, Text } from 'react-native';

// Worker (tablero+perfil móvil): perfil user/guide.
export function ProfileScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-bg px-5">
      <Text className="font-display text-2xl text-ink">Perfil</Text>
      <Text className="mt-2 text-ink-2">Perfil (móvil) — pendiente.</Text>
    </View>
  );
}
