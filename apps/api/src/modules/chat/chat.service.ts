// chat.service — historial paginado, enviar (REST + emite socket), borrado suave (deletedAt).
// Worker: usa MessageModel, TripModel (pertenencia); realtime/emit a la sala del trip.
export const CHAT_MODULE = 'chat';
