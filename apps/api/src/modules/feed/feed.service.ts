// feed.service — feed de inicio: activities de a quién sigues + propias, por recencia,
// respetando visibilidad (UC-H1).
// Worker: usa ActivityModel, FollowModel; pagina por recencia.
export const FEED_MODULE = 'feed';
