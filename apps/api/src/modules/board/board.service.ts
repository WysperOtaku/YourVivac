// board.service — pines (unión por tipo), sanea markdown, unfurl de links,
// mover/editar layout, reacciones; incrementa trip.stats.pinCount; emite por Socket.IO.
// Worker: usa PinModel, TripModel; realtime/emit; reglas de pertenencia/propiedad.
export const BOARD_MODULE = 'board';
