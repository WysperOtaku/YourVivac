// auth.controller — handlers Express que adaptan req/res al auth.service.
// Worker: exporta handlers con `asyncHandler` y conéctalos en auth.routes.ts.

export const __authControllerPlaceholder = true;
