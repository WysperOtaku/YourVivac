// users.service — perfil, settings, avatar (sharp→Cloudinary), follow/unfollow idempotente.
// Worker: usa UserModel, FollowModel, TripModel, TipModel, MediaModel, lib/images, lib/cloudinary.
export const USERS_MODULE = 'users';
