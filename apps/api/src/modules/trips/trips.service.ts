// trips.service — CRUD, invite (idempotente), rsvp, complete (stats+activity), explore.
// Worker: usa TripModel, ActivityModel, UserModel; slug con @yourvivac/utils; reglas §3/§4.
export const TRIPS_MODULE = 'trips';
